{
  "name": "ssm-env",
  "version": "0.0.1",
  "description": "Grab SSM parameters and put them into the environment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Craig Cooper",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "prettier": "^2.0.5"
  }
}
